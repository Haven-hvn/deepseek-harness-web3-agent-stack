{
  "name": "dsh-haven-workspace",
  "private": true,
  "description": "Development workspace for the dsh-haven plugin family: dsh-wallet, dsh-wallet-ethereum, dsh-treasury, dsh-channel-xmtp, dsh-storage-synapse. Each package is an independently installable dsh bundle; this root exists only for shared dev tooling.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "tsc -b --noEmit || tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
