// Persona is config-only via cordis.patch.yml system-prompt row.
// This bundle exists to make persona versioned in repo, not a profile patch.
// No runtime code needed — the patch is the product.
export const name = 'persona-placeholder'
